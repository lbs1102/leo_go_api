name: Go
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: 
      - self-hosted
      - leo-work-runner
      
    steps:
      - name: Run exe
        run: |
          cd D:/leo_go_api
          Start-Process leo_go_api.exe
        shell: cmd
